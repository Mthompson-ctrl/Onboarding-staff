// Placeholder for M2.2 — replaced in M2.3 with the real "Hello, candidate"
// page that exercises @sentinel/shared (forces a build-time failure if
// the workspace wiring isn't carrying into mobile).
export default function Home() {
  return (
    <main className="px-4 py-8">
      <h1 className="text-2xl font-semibold">Sentinel HR</h1>
    </main>
  );
}
